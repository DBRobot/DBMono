#ifndef DB_CAN_PROTOCOL_H
#define DB_CAN_PROTOCOL_H

/*
 * THIS FILE IS AUTOGENERATED CODE
 * DO NOT EDIT
*/

#include <stdint.h>
#include <stdbool.h>

typedef enum {
    /* @@PERM_FLAGS@@ */
} reg_perm_t;

/* result of every command primitive; register-specific errors are >= 64 */
typedef enum {
    /* @@CMD_RESULT@@ */
} cmd_result_t;

/* standard (global) error codes, 0..63; register-specific errors are >= 64 */
typedef enum {
    /* @@ERROR_CODES@@ */
} reg_err_t;

/* @@PROTOCOL_ENUM_TYPES@@ */

#endif
